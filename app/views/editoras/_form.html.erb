<%= form_with(model: editora) do |form| %>
  <% if editora.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(editora.errors.count, "error") %> prohibited this editora from being saved:</h2>

      <ul>
        <% editora.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
