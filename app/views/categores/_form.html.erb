<%= form_with(model: categore) do |form| %>
  <% if categore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categore.errors.count, "error") %> prohibited this categore from being saved:</h2>

      <ul>
        <% categore.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
