

<div class="categdespesas index">
<h2>Categorias</h2>
<%= form_tag categores_path( format:"html"), :method => 'get' do %>  
<div class="row">
  <div class="col-lg-12">
    <div class="row">
        <div class="col-lg-5  form-group">  
          <%= text_field_tag :nome, params[:nome], :class => 'form-control', :placeholder=>"Digite o nome para pesquisar" %>  
        </div>
        <div class="col-lg-2 form-group">
          <%= submit_tag "Pesquisar", :class => "btn btn-primary colorprimarybtn" %>
        </div>
      
        <div class="col-lg-2 form-group">  
            <span class="input-group-btn" style="display: inline-block;"><button class="btn btn-primary colorprimarybtn" type="button" onclick="javascript: location.href='<%= new_categore_path %>';"><i class="fa fa-plus-circle"></i> Novas Categorias</button></span>
      </div>
  </div>
</div>
</div>
<% end  %>  

<div class="table-responsive">
<table class="table table-hover table-striped" cellpadding="0" cellspacing="0">
<thead>
<tr>
<th>COD</th>
<th>Nome</th>
<th colspan="3"></th>
</tr>
</thead>

<tbody>
<% @categores.each do |categore| %>
<tr>
  <td><%= "%06d"%categore.id %></td>
  <td><%= categore.nome %></td>
  <td><%= link_to 'Visualizar', categore %></td>
  <td><%= link_to 'Alterar', edit_categore_path(categore) %></td>   
  <td><%= link_to 'Excluir', categore, method: :delete,remote: true, class: 'remove-tag', data: { confirm: 'Deseja excluir a Categoria?' } %></td>
</tr>
<% end %>
</tbody>
</table>
</div>

<%= paginate @categores%>
</div>